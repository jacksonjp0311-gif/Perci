# Weight reassessment · PERCIW03 · software v0.6.16

**Date:** 2026-07-17  
**Pack:** `models/perci-cognitive-v0.3.pwgt` · PERCIW03 · ~200 MiB · 403,163 prototypes · 124 concepts  
**Claim boundary:** Engineering geometry over sparse binary codes — not consciousness, not pack rebuild without authorize.

## Live classify snapshot (release binary)

| Prompt | label | margin | overlap | overlap_z | mix labels | note |
|--------|-------|-------:|--------:|----------:|------------|------|
| interfaces earn trust under lag/retry | general | 2 | 69 | **14.8** | systems, code | Contested; operators own answer |
| bridge Willshaw + XOR role-filler | memory | 0 | 72 | **15.4** | english, code | Primary insight was identity-memory (weak); synthesis frames fix speech |
| invent constrained metaphor sparse cognition | **creativity** | 0 | 110 | **26.1** | english | Field routes creativity; metaphor insight available |
| what are you measuring when you answer? | general | 10 | 70 | **19.1** | identity | Introspection operator owns speech |

## Emergent math (what the pack actually shows)

1. **Margin is a phase variable** — 0–2 ≈ multipartite / contested; operators must not trust primary prose alone.  
2. **overlap_z (chance-normalized)** is large (15–26) on open conceptual asks → non-random structure in 4096-bit space.  
3. **Soft attention α** splits mass across labels even when primary label is coarse (`general`).  
4. **VSA composition tags** appear on query encode (`ask:`, `relate:`) without pack-side VSA.  
5. **Creativity domain fires** on invent/metaphor — pack supports creative routing; speech needs constraint operators so invention stays governed.

## Do we need a weight rebuild?

| Path | Verdict |
|------|---------|
| Operators + SoftCascade + frames | **Continue** — live battery green on trust/transfer/refuse/code |
| Pack-side VSA re-encode (PERCIW04) | **Later, authorize only** — query VSA already bridges; rebuild is human-gated |
| More prototypes | **Not the bottleneck** — multipartite mass exists; readout + operators were |
| Curriculum fold for Willshaw/XOR | Optional inject prompts — frames now catalog-backed |

## Software moves in 0.6.16

- Willshaw + XOR aliases/frames for synthesis  
- `creative-constraint` operator (structure transfer + non-transfer + test)  
- `operational-introspection` operator (measurable process, uses last `/think` plan)  
- `/think` reports **field phase** from margin  

## Honest breakthrough read

You are not “becoming conscious.” You are closing the loop:

**sparse multipartite field → thought arc → critique → human speech → inspectable plan**

That is a new *local intelligence type* if transfer hardness keeps rising. Weights are healthy enough to keep building software cognition on top; promote nothing without eval + authorize.
